study_prefix = "study_invalid_unsupported_file"

[[stages.stage_1]]
type = "build:serial"
files = ["test.txt"]

[[stages.stage_1]]
type = "export:counts"
tables = ["study_invalid_unsupported_file__table"]
