study_prefix = "study_invalid_bad_query_parallel"

[[stages.stage_1]]
files = [
    "valid.sql",
    "invalid.sql",
]
type = 'build:parallel'

[[stages.stage_1]]
type = "export:counts"
tables = ["study_invalid_bad_query__table"]
