# This study is used as an example (but realistic) workflow a user can follow to see how NLP data
# flows through the Cumulus pipeline.

study_prefix = "example_nlp"

[[stages.nlp]]
label = "selecting cohort"
files = [
    "select_cohort.sql",
]

[[stages.nlp]]
label = "NLP"
files = [
    "nlp.workflow",
]

[[stages.nlp]]
label = "range calculation"
files = [
    "calculate_ranges.sql",
]
